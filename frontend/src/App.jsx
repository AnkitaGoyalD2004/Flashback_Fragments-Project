import { RouterProvider, createBrowserRouter } from "react-router-dom";
import Form from "./components/Form";

const App = () => {
  return (
    <RouterProvider>
      <Form />
    </RouterProvider>
  );
};

export default App;
